<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
	<head>
		<title><%= @page_title ? "#{@page_title} | Heaven's Best Directory" : "Heaven's Best Directory" %></title>
		<meta http-equiv="content-type" content="text/html; charset=utf-8" />
		<meta name="description" content="" />
		<META name="keywords" content="carpet cleaning, upholstery cleaning, tile and grout cleaning, hardwood cleaning, 
		cleaning experts, dry in one hour, Heaven's Best, heavens best carpet cleaning, stain removal, expert cleaning">
		<%= stylesheet_link_tag 'reset', 'main', 'navigation', '960', :cache => true %>
		<%= javascript_include_tag :defaults, :cache => true %>
		<!--[if lt IE 7]>
			<script src="http://ie7-js.googlecode.com/svn/version/2.0(beta3)/IE7.js" type="text/javascript"></script>
		<![endif]-->
	</head>
	<body>

	<div id="container" class="container_16">
		<div id="p-header">	
			<div class="logo"><a href="/"><div class="logo"></div></a></div>		
			<% if @account %>
				<%= render :partial => "shared/area_menubar" if @account %>
			
				<div id="contact" class="grid_4">
					<span class="phone">
						<%= number_to_phone(@account.phones.first.number, :area_code => true) %></span><br />
					<span class="name"><%= @account.operators.first.name %> </span>
					<span class="title">(<%= @account.operators.first.title %>) </span>
				</div>
				<% else %>
				<%= render :partial => "shared/home_menubar" %>
			<% end %>
		</div>
		
		<%= yield  %>

		<%= render :partial => "shared/footer" %>
	</div>

	<%= render :partial => "shared/analytics" if RAILS_ENV == 'production' %>
	</body>
</html>