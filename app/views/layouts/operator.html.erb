<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
	<title><%= @page_title ? "#{@page_title} | #{APP_CONFIG[:site_name]}" : APP_CONFIG[:site_name] %></title>
	<meta http-equiv="content-type" content="text/html; charset=utf-8" />
	<%= stylesheet_link_tag 'reset', 'main', 'navigation', '960', :cache => true %>
	<%= javascript_include_tag :defaults, :cache => true %>
</head>
<body>

<div class="container_16">

	<div id="header" class="grid_16">
		<a href="/"><div id="admin_logo" class="grid_3"></div></a>
		<% if logged_in? %> 
			<% if admin? %>
				<%= render :partial => "shared/admin_nav" %>
				<% else %>
					<%= render :partial => "shared/user_nav" %>
			<% end %>
			
			<div id="area_options">
				<% if @account ? @account.id : nil %>
					<% link_to "View", area_path(@account.region.abbreviation.downcase, @account.accountlink) %>
					<% link_to "Settings", operator_account_path(@account) %>
				<% end %>
				<%= select_tag(nil, options_for_select([[nil]] + current_user.accounts.to_dropdown, 
					@account ? @account.id : nil), {:class => "grid_4", :onchange => "jumpToArea(this);"} ) %>
			</div>
			
			<%= render :partial => "shared/operator_bar" if @account ? @account.id : nil %>
		<% end %>
	</div>
	<%= render :partial => "shared/operator_tabs" if !logged_in? %>

	<% if flash[:notice] %><div class='notice'><%= flash[:notice] %></div><% end %>
	
	<%= yield  %>
	<div class="clear"></div>

	<%= render :partial => "shared/admin_footer" %>
</div>


<%= render :partial => "shared/analytics" unless ENV['RAILS_ENV'] = 'development' %>	
</body>
</html>