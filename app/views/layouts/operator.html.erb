<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
	<head>
		<title><%= @page_title ? "#{@page_title} | #{APP_CONFIG[:site_name]}" : APP_CONFIG[:site_name] %></title>
		<meta http-equiv="content-type" content="text/html; charset=utf-8" />
		<%= stylesheet_link_tag 'reset.css', 'main', 'navigation' %>
		<%= javascript_include_tag :defaults, :cache => true %>
	</head>
	<body>

	<div id="operator">

		<div id="header">
			<div id="admin_logo"></div>
			<% if logged_in? %> 
				<% if current_user.login == 'nate' %>
					<%= render :partial => "shared/admin_nav" %>
					<% else %>
						<%= render :partial => "shared/user_nav" %>
				<% end %>
				
				<%= select_tag(nil, options_for_select([[nil]] + current_user.accounts.to_dropdown, 
					@account ? @account.id : nil), {:onchange => "jumpToArea(this);"} ) if logged_in? %>
			<% end %>
		</div>
		<%= render :partial => "shared/operator_tabs" unless logged_in? %>

		<% if flash[:notice] %><div class='notice'><%= flash[:notice] %></div><% end %>
		
		<%= yield  %>

		<%= render :partial => "shared/footer" %>
	</div>
	

	<%= render :partial => "shared/analytics" unless ENV['RAILS_ENV'] = 'development' %>	
	</body>
</html>