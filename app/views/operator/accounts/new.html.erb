	<h1><%= @page_title = 'Create a new area' %></h1>

	<% form_tag :action => 'create' do %>
	  <% if @account.errors.any? %>
	    <div class="errorExplanation">
	      <h2>Errors encountered with your area information:</h2>
	      <ul><%= @account.errors.full_messages.map {|m| content_tag('li', m)} %></ul>
	    </div>
	  <% end %>
  
	  <fieldset><legend>Area Information</legend>
	    <% fields_for :account do |f| %>
    
		<div><%= f.label :plan_id, 'Plan' %>
			 <span class="plan"><% render :partial => "plans" %></span>
			 <%= select_tag :plan_id, options_from_collection_for_select(SubscriptionPlan.all, 
				:id, :to_s ) %></div>

		<%= render :partial => 'fields', :locals => {:f => f} %>   

	    <% end %>
	  </fieldset>
  
	  <% if @account.needs_payment_info? %>
	  <% end %>
  	  <fieldset><legend>Payment Information</legend>
	    <%= render :partial => "shared/credit_card_form" %>
	  </fieldset>

	  <br />
	  <label>&nbsp;</label>
	  <input type="submit" value="Create my area" />
	<% end %>

	<div id="plans"><% render :partial => "plans" %></div>
	<div class="clearfix"></div>
