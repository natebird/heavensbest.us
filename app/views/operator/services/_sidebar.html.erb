<div id="sidebar" class="grid_4">
	<h1>Services</h1>
		<ul>
			<% for service in @services %>
				<%= (link_for service.title, edit_operator_account_service_path(@account, service), "").html_safe %>
			<% end %>
			<li class="add"><%= link_to 'Add a Service', new_operator_account_service_path(@account) %></li>
		</ul>
</div>