<% form_for [:admin, @photo], :html => { :multipart => true } do |f| %>
  <%= f.error_messages %>

  <p>
	<%= f.label :name %>
	<%= f.text_field :name %></p>
	
  <p>	
	<%= f.label :image %>
	<%= f.file_field :image %></p>
    
  <p>
    <%= f.submit "Save" %></p>

<% end %>
