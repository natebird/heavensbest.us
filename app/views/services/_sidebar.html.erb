<div id="sidebar">
	<h1>Services</h1>
		<ul>
			<% for service in @services %>
				<li><%= layout_link_to service.title, 
						service_path(params[:region], params[:account], @services.first.title) %></li>
			<% end %>
		</ul>
</div>